%table.table.table-striped
  %thead
    %tr
      %th Nombre
      %th Personería
      %th Origen
      %th.money Acciones
      %th.money Participación
  %tbody
    - @shareholders.each do |shareholder|
      %tr
        %td= shareholder.name
        %td= shareholder.personhood
        %td= shareholder.source
        %td.money= shareholder.shares
        %td.money= shareholder.stock.to_s + '%'
